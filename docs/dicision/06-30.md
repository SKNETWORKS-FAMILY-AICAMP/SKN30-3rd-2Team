# 의사결정 로그 — 2026-06-30

> [고도화 G] 레버 비교(Z_Lever) 철회 및 서브청킹(G_sub_chunk) 설계 확정 세션에서 내린 결정 모음.
> 맥락: 오버엔지니어링으로 판정된 Z_Lever(element 정렬·법률 레버 룰 비교) 대신, 서브청크 + 부모 롤업 + 커버리지 체크로 동일 문제를 단순하게 해결하는 G_sub_chunk 설계를 검토·확정.
> 원칙 출처는 [기획서](../01.mvp_기획.md)·[AGENTS.md](../../AGENTS.md). 전날 결정은 [06-29.md](06-29.md).

---

## 0. 세션 결론 (한 줄)

Z_Lever 전면 철회, [G_sub_chunk.md](../tasks/G_sub_chunk.md) 설계를 **"Max Roll-up + 커버리지 체크"** 구조로 확정. 동결 계약 무변경, DB는 가법 신설(`standard_sub_chunks`)만 추가. 문서 전면 반영 완료.

---

## 1. Z_Lever 설계 전면 철회

- **결정: 구 Z_Lever(element 정렬·4대 법률 레버 룰·심각도 결정표) 설계 폐기.**
- **이유:**
  - element 정렬 → 1:N/N:1 허용을 위해 임계값 다대다 매칭을 채택해도, 표준 element 별 정렬·레버 비교까지 가면 "어디가 어떻게 불리한가" 해석이 1차 코드 안으로 들어온다 → 규칙 #1(1차 LLM·해석 금지) 경계 위반.
  - 동결 계약 변경 3건(granularity·change_kind·ElementResult 신설) + standard_elements 테이블·신규 enum 승인이 선행 조건이었으나, 구조 복잡도 대비 효용이 검증되지 않음.
  - **1차 MVP의 역할은 "어느 조항이 CHANGED되었는지 정확히 탐지"이며, "어디가 어떻게 불리한지"는 2차 LLM 역할.** Z_Lever는 이 경계를 침범했음.
- **조치:** 동결 계약 변경 승인 안건 전면 철회. [G_sub_chunk.md](../tasks/G_sub_chunk.md)로 대체.

---

## 2. 임베딩 희석 문제의 정확한 분석

- **결정: "임계값(`change_threshold`)을 올리면 항 삭제를 탐지할 수 있다"는 가정을 폐기.**
- **이유:** 10개 항 중 1개 삭제 시 나머지 9개가 표준과 일치 → 전체 유사도 ≈ 0.97. 임계값을 0.99로 올려도 통과. 임계값은 marginal 케이스 보조 수단이지 삭제 탐지 메커니즘이 될 수 없음.
- **채택: 서브청크 커버리지 체크**가 삭제 탐지의 주 메커니즘.

---

## 3. Roll-up 스코어 집계 방식 — Max 확정

- **결정: 부모 조항 확보(retrieval) 단계의 롤업은 Max Score로 고정. Mean은 기각.**
- **이유:** 거대 조항에서 사용자가 특정 항만 언급할 경우, Mean은 나머지 항의 낮은 점수에 묻혀 올바른 부모를 놓친다. Max("하나라도 잘 맞으면 후보 부모")가 retrieval 목적에 맞음.
- **Mean이 필요한 상황 없음:** 롤업은 부모 확보 전용. 삭제 탐지는 이후 커버리지 체크(불리언)가 담당하므로 여기서 평균을 낼 이유가 없어짐.

---

## 4. 커버리지 체크 — 삭제 탐지의 주 메커니즘 채택

- **결정: 부모 매칭 후, 해당 부모의 표준 서브청크 각각에 대해 사용자 서브청크 중 임계값 이상 매칭이 하나라도 있는지 확인. 미커버 서브청크 존재 시 → `Deviation.CHANGED` 상향.**
- **기존 로직과 방향이 반대임을 명시:**
  - 기존(retrieval): 사용자 → 표준 서브청크 탐색
  - 커버리지 체크: 표준 서브청크 각각 → 사용자 서브청크에서 대응 확인
- **장점:** Z_Lever의 element 정렬 없이도 항 삭제를 결정론적으로 탐지. 동결 계약 변경 불필요.

---

## 5. 커버리지 체크 범위 — 조 내부(clause-local)로 한정

- **결정: 커버리지 체크는 해당 조 내 사용자 서브청크만 대상으로 한다. 전체 계약 교차 탐색(option b)은 채택하지 않는다.**
- **판단 배경:** 사용자가 표준 ⑩을 다른 조에 흩어 적었을 경우, 조 내부 커버리지만으로는 "삭제"와 "재배치"를 구분할 수 없다.
- **이유 (option b 기각):**
  1. 전체 교차 탐색도 정답을 보장하지 않는다 — 다른 조의 유사 문구가 오매칭될 수 있어 오히려 노이즈 증가.
  2. "흩어짐이 허용되는가"는 조문 위치·효력 해석의 문제 → 2차 LLM이 계약 전체 맥락에서 판단해야 할 영역. 1차가 흡수하면 규칙 #1 경계를 침범.
- **결론:** "표준 ⑩ 미커버 → CHANGED → 2차로 넘김" 흐름이 1·2차 역할 분담의 정확한 구현. option a의 false positive는 2차 입장에서 valid input.

---

## 6. 거대 조항 분할 조건 확정 (초기값)

- **결정: 500자 초과 OR 항·호 기호(①②, 1. 2. 등) 3개 이상이면 거대 조항으로 판정, 항 단위 서브청크 분할.**
- **위상:** 초기값. 코퍼스 빌드 후 측정으로 조정 가능. 단, 설계 문서에 제안값이 명시되어야 한다는 원칙 적용.

---

## 7. DB 스키마 변경 — 가법 신설 (동결 계약 무변경)

- **결정: 기존 3개 테이블(`standard_clauses`·`clause_relations`·`toxic_patterns`) 및 모든 기존 모델·enum·MCP 시그니처 무변경. 아래 2건만 순수 신설.**

| 신설 | 형태 | 역할 |
|---|---|---|
| `standard_sub_chunks` 테이블 ([01.CREATE_TABLE.sql](../../data/migration/01.CREATE_TABLE.sql)) | SQLite 신설 | 커버리지 체크 시 `WHERE parent_clause_id=?` 열거용 |
| `StandardSubChunk` 모델 ([models.py](../../src/contracts/models.py)) | Pydantic 신설 | 서브청크 데이터 표현 |

- **SQLite에도 저장하는 이유:** Chroma의 `where` 필터는 벡터 검색에 최적화되어 있어 특정 부모의 서브청크 전체 목록 열거에 부적합. 구조적 조회는 SQLite가 맞는 역할.
- **`DeviationResult`·MCP 시그니처 불변** → 기존 소비자(server·dto) 코드 수정 불필요.

---

## 8. 평가 지표 명확화

- **결정: G 고도화의 주 측정 지표를 Coverage Recall로 명확화.**
- **정의: 삭제된 표준 서브청크를 "미커버"로 잡아내는 비율.**
- **이유:** 기존 Recall@k(retrieval용)는 "올바른 부모 조항을 찾는가"를 측정하며, "삭제된 항을 탐지하는가"와는 다른 지표. G의 핵심 기여는 항 삭제 탐지이므로 Coverage Recall이 주 지표.
- **LLM-judge 미도입** (규칙 #5 준수), F1·Recall 등 결정론적 수치만 산출.

---

## 9. 기존 작업 영향 요약

| 파일 | 강도 | 내용 |
|---|---|---|
| `01.CREATE_TABLE.sql` | 🟠 신설 | `standard_sub_chunks` 테이블 |
| `models.py` | 🟠 신설 | `StandardSubChunk` 모델 |
| `0.migrate.py` | 🟠 확장 | 서브청크 생성·적재 단계 추가 |
| `build_index.py` | 🟠 확장 | `build_sub_chunk_index()` 추가 |
| `vector_manager.py` | 🟠 확장 | `rollup_search()` 추가 |
| `review_pipe.py` | 🟠 확장 | 커버리지 체크 조립 (현재 NotImplementedError → 처음부터 포함 설계 가능) |
| `core/coverage.py` | 🟠 신설 | 커버리지 체크 순수함수 (TDD 대상) |
| `tests/pipe/test_build_index.py` | 🟠 확장 | 서브청크 빌드·검색 테스트 추가 |
| `eval/ablation.py` | 🟠 확장 | Coverage Recall 비교 함수 추가 |
| `enums.py`·`ports.py`·`server/`·`core/` 기존 | 🟢 무변경 | `Retriever.search`가 이미 `collection_name` 인자를 받아 서브청크 컬렉션 재사용 가능 |

**구현 권장 순서:** SQL + models → migrate → build_index → vector_manager rollup → core/coverage → review_pipe → ablation.

---

## 후속(미결) 항목

- `0.migrate.py` 서브청크 생성 로직 구현 — 분할 조건(500자/3개) 적용 및 `standard_sub_chunks` INSERT.
- `build_index.py` `build_sub_chunk_index()` 구현 및 통합 테스트.
- `vector_manager.py` `rollup_search()` 구현.
- `core/coverage.py` 커버리지 체크 순수함수 구현 (TDD — 테스트 먼저).
- `review_pipe.py` C 태스크 구현 시 커버리지 체크 포함 (팀원 C/리드 담당).
- 거대 조항 "항 삭제" 변형 골든셋 구축 + Ablation Before/After Coverage Recall 증명.

---

---

# 배포 아키텍처 · 모델 선택 세션 결정 모음

> 맥락: 팀원 기능 구현 병행 중, 시연 목적 배포 환경 구성 방향을 확정한 세션.

---

## 10. 시연 배포 아키텍처 — Cloud Run 단일 컨테이너

- **결정: 시연용 배포는 Docker 단일 컨테이너, Google Cloud Run으로 확정.**
- **구성:**
  - MCP 서버 (Node + Python), kordoc·korean-law-mcp 내장
  - SQLite + Chroma embedded 포함
  - 임베딩·리랭커는 외부 API 호출 (별도 모델 서버 불필요)
- **기각된 옵션:**
  - VM 분리(모델 서버 + MCP 서버): 두 서비스 관리 부담, 시연 규모 초과
  - infinity-emb 자체 호스팅: 4GB RAM VM 필요, 시연 대비 과도한 비용
- **이유:** 단순 시연이므로 인프라 복잡도를 최소화. 추후 실서비스 전환 시 모델 서버 분리.

---

## 11. 임베딩·리랭커 모델 — HF Serverless API + 리랭커 no-op

- **결정: 시연 기간 동안 임베딩은 HF Inference API, 리랭커는 no-op(통과)으로 처리.**
- **이유:**
  - bge-reranker-v2-m3 전용 rerank 엔드포인트가 HF Serverless API에 없음. text-classification으로 우회 시 배치 처리 복잡도 증가.
  - 시연 규모(수백 건 조항)에서 리랭커 제거로 인한 정확도 저하가 미미.
- **구현:** `adapter/reranker.py` no-op 구현 — candidates 그대로 반환.
- **주의:** HF API 콜드 스타트 ~20초. 시연 전 워밍업 호출 필요.
- **전환 조건:** 실서비스 또는 정확도 검증 시 infinity-emb VM으로 교체. `adapter/embedder.py` 모델명·엔드포인트 환경변수만 교체하면 됨.

---

## 12. bge-m3 선택 근거 재검토 — dense 단독 사용 확인

- **결정: 현재 구현이 bge-m3의 dense 출력만 사용함을 확인. bge-m3 고유 이점(sparse·ColBERT)이 활용되지 않고 있음.**
- **분석:**
  - Dense: Chroma에 저장 → 사용 중.
  - Sparse (lexical weight): BM25(Kiwi)로 대체 중 → bge-m3 sparse 미사용.
  - ColBERT (multi-vector): Chroma가 문서당 벡터 1개만 지원 → 구조적으로 저장 불가.
- **결론:** dense만 쓰면 bge-m3는 크고 느린 일반 임베딩 모델과 동일. 경량 한국어 모델과 비교 평가 필요.

---

## 13. 임베딩 모델 비교 평가 계획

- **결정: `BAAI/bge-m3` vs `jhgan/ko-sroberta-multitask` Hit@1·Hit@3 비교 후 모델 확정.**
- **방법:**
  1. `data/03_normalized/standard_clauses.*.json` 기반 테스트 쌍 20~30개 수작업 구성 (`eval/clause_pairs.json`).
  2. 리랭커 없이 dense 검색 단독으로 비교 (BM25는 모델 무관이므로 변수 격리).
  3. `eval/compare_embedders.py` 스크립트로 Hit@1·Hit@3 산출.
- **판단 기준:**

  | Hit@1 차이 | 결론 |
  |---|---|
  | 5% 이하 | ko-sroberta 채택 (HF API 콜드스타트·크기 우위) |
  | 5~10% | 시연이면 ko-sroberta, 실서비스면 bge-m3 |
  | 10% 초과 | bge-m3 유지 |

- **후속:** 비교 결과에 따라 `adapter/embedding_model.py` 모델명만 교체.

---

## 14. Chroma 다중 인덱스 — collection_name 파라미터로 이미 지원

- **결정: `VectorManager`가 이미 `collection_name` 인자 기반으로 다중 컬렉션을 지원함을 확인. 추가 설계 변경 불필요.**
- **구조:**
  ```
  PersistentClient
  ├── "standard_clauses"   ← 표준조항
  └── "toxic_patterns"     ← 독소패턴 (build_toxic_index 추가만 하면 됨)
  ```
- **BM25 캐시도 `(collection_name, filter_tuple)` 키로 컬렉션 간 격리됨** — 기존 코드 수정 없이 컬렉션명만 다르게 넘기면 독립 인덱스로 동작.
- **ColBERT 불가 확인:** Chroma는 문서당 벡터 1개만 지원. ColBERT(토큰당 N벡터)가 필요하면 Qdrant·LanceDB로 DB 교체 필요. 시연 범위 내에서는 현행 유지.

---

---

# 헥사고날 포트 정리 · 운영 어댑터 구현 세션 결정 모음

> 맥락: 어댑터 종속 포트를 contracts에서 분리하고, 목(mock) 주입이 원활한 구조로 리팩토링. 동시에 로컬 모델 대신 RunPod API를 호출하는 운영용 임베더·리랭커 구현체를 추가.

---

## 15. 포트 위치 결정 — 도메인 포트 vs 어댑터 포트 분리

- **결정: `Embedder`·`Retriever`를 `contracts/ports.py`에서 `adapter/port.py`로 이동. `Reranker`·`Database` 신규 포트도 `adapter/port.py`에 추가.**
- **이유:**
  - `Embedder`·`Retriever`는 벡터 DB·모델 구현에 의존하는 인프라 계약이므로, 도메인 무관 계약(`Parser`·`Grounder`·`Graph`)이 모인 `contracts/`가 아닌 `adapter/`에 귀속시키는 것이 헥사고날 경계에 부합.
  - `contracts/ports.py`는 도메인 행위(파싱·법령 조회·그래프 탐색)만 남겨 순수 도메인 계약으로 유지.
- **기각된 옵션:** `contracts/infra_ports.py` 별도 파일 — 패키지 경계(`contracts` vs `adapter`)가 이미 역할을 표현하므로 불필요한 파일 분할.
- **`markdown_splitter`·`kordoc`·`korean_law_mcp`는 포트 없이 유지:** 각 어댑터가 고유 기능 집합을 가져 Protocol 추상이 실익이 없음. 상위 포트(`Parser`·`Grounder`)가 이미 도메인 경계를 잡고 있어 테스트 격리 충분.

---

## 16. VectorManager — Embedder 생성자 주입 리팩토링

- **결정: `VectorManager.__init__`에 `embedder: Embedder` 파라미터를 추가하고, 내부 싱글톤 직접 import를 제거. 조립 지점을 `adapter/__init__.py`로 일원화.**
- **이전 구조 문제:** `from .embedding_model import embedder`를 모듈 레벨에서 고정 import → `VectorManager` 단독 테스트 시 실제 임베딩 모델이 메모리에 로드됨.
- **변경 후:** `vector = VectorManager(embedder=embedder)` 조립을 `adapter/__init__.py`에서 수행. 테스트에서는 `VectorManager(embedder=MagicMock(spec=Embedder))`로 모델 로드 없이 격리 테스트 가능.
- **기존 사용 인터페이스 무변경:** `from adapter import vector` 패턴은 동일하게 유지.

---

## 17. 운영용 임베더·리랭커 — RunPod Serverless API 채택

- **결정: `app_env != "local"` 환경에서는 `adapter/api_embedding_model.py`의 `ApiEmbedder`·`ApiReranker`를 사용. RunPod `/runsync` 엔드포인트를 HTTP로 호출.**
- **구현 방식:**
  - `ApiEmbedder.embed_documents` → POST `/runsync`, `output.data[].embedding` 추출 후 `index` 정렬로 입력 순서 보장.
  - `ApiReranker.compute_scores` → POST `/runsync` (`return_docs: false`), `output.scores[]` 추출.
  - RunPod 응답 외피(`output` 키) 언래핑은 `_call_runsync()` 내부에서 처리 — 호출부에 노출 안 됨.
- **콜드 스타트 대응:** `requests.post` 타임아웃 60초 고정 (RunPod cold-start 최대치).
- **환경변수:** `RUNPOD_API_KEY`·`RUNPOD_ENDPOINT_ID` — `config.py`에 추가, `.env`로 관리.

---

## 18. app_env 기반 구현체 선택 — adapter/__init__.py 조립

- **결정: `adapter/__init__.py`에서 `app_env == "local"` 분기로 로컬 모델·API 구현체를 선택. 외부 소비자 코드는 변경 없음.**

  ```python
  if app_env == "local":
      from .embedding_model import embedder, reranker
  else:
      from .api_embedding_model import api_embedder as embedder, api_reranker as reranker
  ```

- **이유:** 환경별 구현체 교체가 단일 지점(adapter 조립)에서 일어나므로, pipe·server·core는 `app_env` 를 알 필요가 없음. 헥사고날 원칙의 "코어는 외부를 모른다" 유지.
- **전환 조건:** `.env`의 `APP_ENV` 값 변경만으로 로컬 ↔ 운영 전환 가능.

---

## 기존 작업 영향 요약 (15~18)

| 파일 | 강도 | 내용 |
|---|---|---|
| `adapter/port.py` | 🟠 신설 | `Embedder`·`Retriever` 이동 + `Reranker`·`Database` 신규 |
| `contracts/ports.py` | 🟡 축소 | `Embedder`·`Retriever` 제거, 도메인 포트만 유지 |
| `adapter/vector_manager.py` | 🟡 리팩토링 | 생성자 주입(`embedder: Embedder`), 모듈 싱글톤 제거 |
| `adapter/__init__.py` | 🟡 변경 | `VectorManager` 조립 + `app_env` 분기 추가 |
| `adapter/api_embedding_model.py` | 🟠 신설 | `ApiEmbedder`·`ApiReranker` RunPod HTTP 구현체 |
| `config.py` | 🟡 확장 | `RUNPOD_API_KEY`·`RUNPOD_ENDPOINT_ID` 환경변수 추가 |
| `pipe/review_pipe.py` | 🟢 import 경로만 수정 | `Retriever` → `adapter.port` |
| `tests/pipe/test_review_pipe.py` | 🟢 import 경로만 수정 | 동일 |

---

---

# 카테고리 라벨링 개선 · GENERAL 캐치올 도입 세션 결정 모음

> 맥락: SI/SM 하도급 표준계약서 정규화에서 `label_category` 카테고리 매칭 품질 저하(WARRANTY 쏠림·표준 조항 SKIP 다발)를 진단하고, 매칭 알고리즘 개선과 분류 체계 공백 해소 방안을 확정한 세션.
> 원칙 출처는 [기획서](../01.mvp_기획.md)·[AGENTS.md](../../AGENTS.md)(동결 계약·"조용한 실패 금지"·결정론적 평가).

---

## 19. 카테고리 대표 벡터 — mean-pooling 폐기, max-pooling 채택

- **결정: `build_category_vectors`는 앵커 평균(중심벡터)을 내지 않고 앵커별 벡터를 그대로 보관한다. `label_category`의 카테고리 점수 = 그 카테고리 앵커 중 가장 가까운 1개와의 코사인 유사도(max-pool).**
- **이유:** 한 카테고리의 앵커가 의미상 여러 갈래일 때(예: `DELIVERY_INSPECTION` = 납품 + 검수) 평균을 내면 어느 쪽에도 안 맞는 중심벡터가 되어, "검사 및 이의신청" 같은 조항이 정작 맞는 카테고리를 놓치고 `WARRANTY`로 쏠렸다.
- **효과:** WARRANTY 캐치올 쏠림·DELIVERY_INSPECTION 누락의 직접 원인 제거.

---

## 20. 쿼리 구성 — 제목 우선 (title 단독 + title+본문, 카테고리별 max)

- **결정: 제목 단독 쿼리와 (제목 + 본문 200자) 쿼리를 각각 임베딩해, 카테고리별로 두 점수 중 더 높은 쪽을 채택한다.**
- **이유:** 실제 법령 조항 본문에는 "손해·배상·책임" 등 일반 법률 어휘가 많아 제목 신호를 희석시킨다. 제목 단독 쿼리는 본문 노이즈를 우회하고, max 결합이므로 본문은 점수를 더할 수는 있어도 끌어내리지 못한다.

---

## 21. 판정 임계값/마진 — 실측 기반 재보정 (0.40 / 0.02)

- **결정: `_SCORE_THRESHOLD = 0.40`, `_MARGIN_THRESHOLD = 0.02`.**
- **이유 (실측):** 합성 테스트 문장은 앵커 단어를 그대로 품어 0.6~1.0으로 튀지만, 형식적 법률 문체의 실제 조항은 0.40~0.55에 깔린다. 초기값 0.55는 실제 조항을 거의 전부 잘랐다.
- **핵심 관찰:** 어디에도 안 맞는 조항("기본원칙" 0.393)을 구분하는 진짜 신호는 절대점수가 아니라 1·2위가 거의 동점(마진 ≈ 0.004)이라는 점. 따라서 임계값은 낮게, 마진은 보조 신호로 둔다.

---

## 22. 표준 조항 SKIP(드롭) 폐기 — "절대 드롭 금지" 원칙

- **결정: 표준 코퍼스 정규화에서 조항을 `ValueError`/`continue`로 버리지 않는다. `normalize_file`의 SKIP 분기 제거.**
- **이유:**
  - `03_normalized`는 **단일 진실원이자 비교 기준선**. 표준 조항을 드롭하면 사용자 계약서에서 해당 조항 누락을 `MISSING`으로 영영 탐지할 수 없는 **조용한 구멍**이 생긴다.
  - AGENTS.md "조용한 실패 금지 / 빈 응답 금지" 위반. 정답 JSON은 모든 조에 카테고리가 달려 있으므로 SKIP은 "올바른 기각"이 아니라 정답 재현 실패.
- **경계 정리:** `NO_MATCH`/예외 기각 의미는 **런타임 사용자 조항 분류**에서만 유효하고, 큐레이션된 표준 코퍼스 빌드에는 적용하지 않는다.

---

## 23. 일반 조항 처리 — GENERAL 캐치올 카테고리 신설 *(동결 스키마 변경 · 승인)*

- **결정: `Category`에 `GENERAL`을 추가한다(앵커 `[]`). 정의·효력발생 시점·통지 방법·계약서 해석 우선순위 등 특정 카테고리에 속하지 않는 일반 조항의 명시적 fallback.**
- **동작:** 앵커가 비어 임베딩 점수 경쟁에 참여하지 않음 → 순수 fallback. 최고 점수가 임계값 미만이면 `GENERAL`로 귀결.
- **이유 (기각: "탐지 제외"):** 22의 "드롭 금지"와 일관. 빈 값/None을 흩뿌리는 대신 `category == Category.GENERAL` 단일 술어로 처리 — 법령 grounding은 `if category == GENERAL: return []`, 의존성 그래프도 동일하게 한 줄 제외. 산발적 예외 처리 방지.
- **참고:** "관할법원"은 이미 `DISPUTE`가 커버하므로 GENERAL 대상은 정의·효력·통지·해석 우선순위 정도로 좁다.

---

## 24. 다중 카테고리 — 단일 라벨 강제 + 구체성 우선순위 *(승인)*

- **결정: 한 조항이 여러 카테고리에 걸치면 단일 라벨만 부여한다. 마진 내로 근접한 복수 후보 중 결정론적 우선순위 목록에서 더 구체적인(사건 중심) 카테고리를 택한다.**
- **우선순위 (인덱스 작을수록 우선):** 특화·사건 중심(`TERMINATION`·`IP_OWNERSHIP`·`CONFIDENTIALITY`·`WARRANTY`·`DELIVERY_INSPECTION`·`SUBCONTRACTING`·`SOCIAL_INSURANCE`·`DISPUTE`·`WORKING_HOURS`·`HOLIDAY_LEAVE`) > 메커니즘·포괄(`PAYMENT`·`LIABILITY`·`SCOPE_SOW`·`CONTRACT_PERIOD`) > `GENERAL`.
- **예:** "해지 시 기성 대금 정산" → `PAYMENT`보다 `TERMINATION`.
- **이유 (기각: 다중 라벨):** MVP 단순성. 다중 라벨은 모델·매칭·평가 로직을 복잡화. 모호함을 마진→예외 기각 대신 우선순위로 **해소**하므로 일관성도 확보(결정론적·재현 가능, AGENTS.md).

---

## 기존 작업 영향 요약 (19~24)

| 파일 | 강도 | 내용 |
|---|---|---|
| `contracts/enums.py` | 🟠 신설 | `Category.GENERAL` 추가 (앵커 `[]`, 캐치올) |
| `pipe/normalize.py` | 🟡 변경 | `build_category_vectors`(앵커별 벡터 보관), `label_category`(max-pool·제목 우선·GENERAL fallback·구체성 우선순위), `normalize_file` SKIP 제거 |
| `contracts/implement/korean_law_grounder.py` | 🟡 변경 | `get_grounding`에 `GENERAL` 단일 술어 제외 |
| `tests/pipe/test_normalize.py` | 🟡 변경 | `test_미분류_조항은_ValueError` → `test_일반조항은_GENERAL` |
| 기존 `models`·`ports`·MCP 시그니처 | 🟢 무변경 | `StandardClause.category`가 enum 추가만으로 수용 |

**전체 테스트:** 44 passed, 4 skipped.

---

## 후속(미결) 항목

- `python -m pipe.normalize` 재실행 → `03_normalized/*.json` 재생성 후 GENERAL 귀결 비율 측정. 비율이 예상과 다르면 임계값(0.40) 조정.
- **option C(분류 체계 공백) 별도 합의 미결:** 안전조치 의무·계약서 교부/추완·하도급대금 결정/감액/조정 등 SI 하도급 특화 조항은 자리가 없어 GENERAL로 빠짐. 앵커 보강 또는 신규 카테고리 추가 여부는 동결 스키마 변경이므로 추가 승인 필요.

---

---

# VectorManager 성능 개선 · 빌드 지연 원인 분석 세션 결정 모음

> 맥락(2026-07-01): `adapter/vector_manager.py`의 하이브리드 검색·BM25 인덱스·적재 경로를 성능 관점에서 점검하고, 공개 시그니처(`Retriever` 포트 포함)·동결 계약 무변경 선에서 내부 구현만 개선. 이어 `build_index.py` 적재가 느린(40s/it) 원인을 측정으로 규명.
> 원칙 출처는 [기획서](../01.mvp_기획.md)·[AGENTS.md](../../AGENTS.md)(동결 계약·"조용한 실패 금지"·결정론적 평가). 검증: 44 passed, 4 skipped, 토큰화 단건/배치 동등성 확인.

---

## 25. hybrid_search — Dense·BM25 병렬 실행

- **결정: `hybrid_search`에서 Dense 검색과 BM25 검색을 `ThreadPoolExecutor(max_workers=2)`로 동시 실행한다.**
- **이유:** 두 검색은 상호 독립적이므로 직렬 실행할 이유가 없다. 지연시간이 `dense + bm25`에서 `max(dense, bm25)`로 단축된다.
- **전제:** embedder(BGE-m3)·Chroma·Kiwi가 forward·I/O 구간에서 GIL을 놓을 때 실효 이득. 순수 파이썬 embedder라면 이득이 제한적이라 실측 권장.

---

## 26. BM25 검색 — 전체 정렬 폐기, heapq.nlargest 채택

- **결정: `bm25_search`에서 점수 전체 정렬(O(N log N)) 대신 `heapq.nlargest(top_k, …)`로 상위 k개만 선별(O(N log k)). dict 복사도 상위 k개에 대해서만 수행.**
- **이유:** `top_k`만 필요한데 전체 코퍼스를 정렬하고 모든 양수-점수 문서를 복사하던 비효율 제거. score > 0(일치 토큰 존재) 필터는 유지.
- **한계:** `bm25.get_scores`는 여전히 전체 코퍼스 순회(O(N))라, 코퍼스가 크면 이 부분이 잔여 병목. 선별 단계만 개선.

---

## 27. BM25 인덱스 빌드 — 락 범위 축소 · NO_MATCH 캐싱 · Kiwi 배치 토큰화

- **결정 (1) 락 범위 축소:** `_get_bm25_index`를 double-checked locking으로 전환. 무거운 빌드(Chroma 조회·토큰화·BM25 생성)는 락 **밖**에서 수행하고, 캐시 조회/저장만 락으로 보호한다.
  - **이유:** 기존엔 빌드 전체가 단일 `self._lock` 안이라, 한 (컬렉션,필터)의 빌드가 **다른 모든 키 조회까지 블로킹**했다. 빌드를 락 밖으로 빼면 동시성이 회복된다. 드물게 두 스레드가 같은 인덱스를 중복 빌드할 수 있으나 결과는 동일(먼저 저장된 것 사용)하므로 정합성 유지.
- **결정 (2) NO_MATCH 캐싱:** 빈 결과를 `None`으로 캐시에 저장한다.
  - **이유:** 기존엔 빈 결과를 캐시하지 않아 빈 컬렉션/필터를 **매 호출마다 Chroma 재조회**. AGENTS.md "빈 응답 금지" 정신과도 맞게, 없음을 명시적으로 캐싱.
- **결정 (3) Kiwi 배치 토큰화:** 인덱스 빌드 시 문서별 직렬 `_tokenize` 대신 `_tokenize_batch`로 전체 코퍼스를 Kiwi에 한 번에 전달(내부 멀티스레드). 영문/숫자 보완 정규식은 모듈 레벨 `_ENG_NUM_RE`로 1회 컴파일.
  - **검증:** 단건 `_tokenize`와 배치 `_tokenize_batch` 결과 동등성 테스트 통과(혼재 한/영/숫자·빈 문자열 포함).

---

## 28. 적재 경로 — embedding/write 파이프라인화 + 캐시 무효화 중복 제거

- **결정 (1):** `add_documents`·`upsert_documents`의 배치 루프를 공통 헬퍼 `_batch_load`로 추출하고, 단일 워커(`ThreadPoolExecutor(max_workers=1)`)로 다음 배치 임베딩 계산과 직전 배치 Chroma 쓰기를 오버랩한다. 쓰기는 단일 워커라 직렬화되어 안전.
- **결정 (2):** add/upsert/delete에 3중 중복이던 캐시 무효화 로직을 `_invalidate_cache(collection_name)` 헬퍼로 통합.
- **유의:** 임베딩이 압도적 병목인 환경(아래 29)에서는 쓰기 시간이 임베딩 대비 미미해 파이프라인 오버랩의 실효 이득이 작다. 구조 정리·안전성 확보 의미가 더 큼.

---

## 29. build_index 적재 지연(40s/it) 원인 — 코드 아닌 CPU 임베딩이 병목

- **결정: 빌드 느림의 근본 원인은 구조가 아니라 "BGE-m3(약 560M) 임베딩 모델을 GPU 없이 CPU로 추론"하는 연산 비용임을 측정으로 확정.**
- **측정 근거:**
  - `torch.cuda.is_available()` → **False** (CPU 추론), `torch.get_num_threads()` = 6.
  - 모델 = `CHKH01/BGE-m3-ko`(BGE-m3 계열, 대형 다국어). `EMBEDDING_MODEL_NAME` 기본값.
  - 로그의 `40.19s/it`는 `SentenceTransformer.encode()` **내부 미니배치(기본 32개)** tqdm. 상위 `batch_size=128`과 무관하게 모델이 다시 32씩 forward → 시간의 95%+ 가 `model.encode()` CPU 연산.
- **부차 요인:** 첫 배치에 1회성 모델 로드(`_load_model`) 비용 포함. #28 파이프라인 오버랩은 이 환경에선 거의 무효(쓰기가 임베딩 대비 무시 수준).
- **해결 방향(미적용, 합의 필요):** ① CUDA GPU 사용, ② 경량 한국어 모델로 교체(CPU 고정 시 체감 최대), ③ `encode` 배치/`convert_to_numpy` 튜닝. → 13(임베딩 모델 비교)·11/17(API 임베더 전환)과 연결.

---

## 미적용(의도적 보류) 항목

- **필터별 BM25 인덱스 통합:** 컬렉션 전체 1개 인덱스 + 검색 시 필터 후처리 방식은 필터 카디널리티에 따라 트레이드오프가 갈리고 동작이 미묘하게 달라질 수 있어 보류. 실제 필터 조합 분포 확보 후 재검토.
- **regex_search/keyword_search 전체 로드:** Chroma가 정규식 미지원이라 코드만으로 해결 불가한 본질적 한계. 호출부에서 `metadata_filter`를 좁히는 운용 규칙으로 완화.

---

## 기존 작업 영향 요약 (25~29)

| 파일 | 강도 | 내용 |
|---|---|---|
| `adapter/vector_manager.py` | 🟡 내부 최적화 | hybrid 병렬화·BM25 nlargest·DCL 락 축소·NO_MATCH 캐싱·Kiwi 배치 토큰화·`_batch_load` 파이프라인·`_invalidate_cache` 통합 |
| 공개 시그니처·`Retriever` 포트·동결 계약 | 🟢 무변경 | `search`/`*_search`/`*_documents` 시그니처 유지 → server·pipe·core 무수정 |
| `build_index.py`·`embedding_model.py` | 🟢 무변경(분석만) | 지연 원인은 CPU 임베딩 — 코드 수정 아닌 자원/모델 선택 문제로 결론 |

---

## 후속(미결) 항목

- 임베딩 CPU 병목 해소 방향 확정: GPU 사용 vs 경량 모델 vs API 임베더(17) — 13의 Hit@1·Hit@3 비교 결과와 연동해 결정.
- hybrid 병렬화 실효 이득 실측(로컬 모델 GIL 점유 여부 확인).
- 필터 조합 분포 측정 후 BM25 인덱스 통합 여부 재검토.
