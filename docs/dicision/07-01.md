# 의사결정 로그 — 2026-07-01

> [review_pipe 취약점 리뷰] `review_contract`(MCP 본체)가 실데이터에서 전건 오작동하던 근본 원인을 규명하고, 검색 매칭 파이프를 재배선한 세션.
> 맥락: `search()`가 주는 RRF `fusion_score`(≤0.033)를 `match_threshold`(0.5)와 직접 비교 + 리랭커 미호출 → 모든 사용자 조항 `NO_MATCH`, 모든 표준조항 `MISSING`. 이 결함 제거를 축으로 리랭커+sigmoid 도입·동결 시그니처 확장·고도화(sub-chunk·toxic·graph) 배선·배치검색까지 처리.
> 원칙 출처는 [기획서](../01.mvp_기획.md)·[AGENTS.md](../../AGENTS.md)(동결 계약·"조용한 실패 금지"·1차 LLM 금지·결정론적 평가). 전날 결정은 [06-30.md](06-30.md). 검증: **56 passed, 2 deselected**.

---

## 0. 세션 결론 (한 줄)

`review_contract` 매칭을 **search → rerank → sigmoid 정규화 → select_best_match** 로 재배선해 전건 오작동을 제거. 동결 시그니처 확장(리랭커 필수 주입 등) 승인. sub-chunk·toxic 배선 + graph seam + 배치검색(`search_many`) 도입. sub-chunk 메타데이터는 **contract_type만 채택(category 보류)**. `check_coverage` 완전 배선은 [H_check_coverage.md](../tasks/H_check_coverage.md)로 분리, `rerank_many` 는 시임만 구현(파이프 미적용).

---

## 1. 매칭 점수 스케일 결함 규명 — 전건 오작동의 근본 원인

- **진단: `hybrid_search`가 반환하는 `fusion_score`(RRF)는 이론상 최댓값 ≈0.033(=2/61)인데, 이를 `match_threshold=0.5`와 직접 비교했다. 또한 아키텍처가 전제한 리랭커 단계가 코드에서 통째로 누락됐다.**
- **결과:** `select_best_match`가 항상 `(None, 0.0)` 반환 → 모든 사용자 조항 `NO_MATCH`, matched_ids 공집합 → 모든 표준조항 `MISSING`. 테스트가 통과한 건 `FakeRetriever`가 `rerank_score=0.95`를 하드코딩했기 때문.
- **불변식 확정:** 매칭 판정에는 **반드시 Reranker를 통과한 `rerank_score`만** 쓴다. `fusion_score`(랭크 기반)는 threshold와 스케일이 달라 매칭 비교에 절대 쓰지 않는다.

---

## 2. 매칭 점수 정규화 — 리랭커 + sigmoid *(둘 다 채택)*

- **결정: `rerank_score`(BGE 리랭커의 raw 로짓)를 `core.sigmoid`로 0~1 정규화해서 threshold와 비교한다.**
- **이유:** `CrossEncoder.predict`는 정규화되지 않은 로짓(음수~양수)을 반환한다. sigmoid(로짓 0)=0.5 이므로 **`match_threshold=0.5` = "모델이 양(+)의 판단"** 이라는 해석이 유지된다.
- **배치:** `sigmoid`는 순수함수라 `core/matching.py`에 두고 export(오버플로 방지 위해 부호 분기). 정규화 지점은 `pipe`(조립).
- **기각 안 함:** "fusion_score 정규화" 단독안(리랭커 미사용)은 매칭 정밀도가 낮아, **리랭커 도입 + sigmoid 정규화를 함께** 채택.

---

## 3. 동결 시그니처 확장 승인 — `review_contract`

- **결정: MCP 동결에 가까운 `review_contract` 시그니처를 확장한다(AGENTS.md §2에 따라 사람 승인 후 진행).**
- **추가 파라미터:** `reranker: Reranker`(필수), `graph: Optional[Graph]=None`, `change_threshold`, `toxic_threshold`, `top_k`, `use_sub_chunk`, `use_toxic`.
- **이유:** 리랭커 없이는 매칭이 성립하지 않으므로 **필수 주입**으로 만들어 스케일 버그 재발을 구조적으로 차단. `graph`는 구현체 부재로 옵션(seam). 나머지는 eval ablation 스위치.
- **호출부 갱신:** [server.py](../../src/server/server.py) `review_contract_pipe(..., reranker=reranker)`.

---

## 4. NO_MATCH vs EXTRA 분기 정정 — EXTRA 경로 복원

- **결정: `hits`가 아예 없을 때만 `NO_MATCH`, 후보는 있으나 임계 미달이면 `classify_clause_deviation`으로 넘겨 `EXTRA`로 판정한다.**
- **이유:** 기존 코드는 `select_best_match`가 None이면 무조건 NO_MATCH로 보내 **EXTRA가 도달 불가능**했다. core 규격(README)은 "후보 있으나 전부 threshold 미달 = EXTRA / 검색이 후보를 못 냄 = NO_MATCH"로 구분한다.

---

## 5. grounding 게이팅 — 이탈(CHANGED/MISSING)에만, GENERAL 제외

- **결정: 법령 근거는 `CHANGED`·`MISSING` 이탈 조항에만 부착. `NONE`/`EXTRA`/`NO_MATCH`는 미부착. `Category.GENERAL`은 grounding 대상 아님.**
- **이유:** 기존엔 매칭된 전건(NONE 포함)에 grounding을 호출해 낭비·스펙 불일치. pipe README 5단계 및 06-30 §23(GENERAL 캐치올) 정합.

---

## 6. 고도화 배선 — sub-chunk roll-up · toxic 역방향 · graph seam

- **결정 (sub-chunk):** `core.roll_up_sub_chunks`를 export하고 파이프에 배선. 서브청크 검색 결과를 `parent_clause_id` Max로 롤업해 부모 표준조항 후보로 채택(거대 조항 recall 보강, G_sub_chunk 2단계). `core.check_coverage`도 export만 함(배선은 §11로 분리).
- **결정 (toxic):** `core.detect_toxic_patterns` 배선. **매칭 성패와 무관하게 항상** 독소 역방향 검색 수행(표준엔 없지만 해로운 EXTRA 조항 포착).
- **결정 (graph):** `graph`는 옵션 주입 seam만. **`ClauseGraph(Graph)` 구현체가 아직 없어**(`contracts/implement/`에 grounder만 존재) `related_risk_clauses`는 현재 비활성 → 후속(F_graph).

---

## 7. 배치 검색 — `Retriever.search_many` 신설

- **결정: 조항별 개별 `search`(N회)를 컬렉션당 1회 `search_many`로 축소한다. 포트에 `search_many`를 노출하고 `VectorManager`에 concrete 구현.**
- **이유:** 조항마다 `embed_query`가 직렬화돼 임베딩 왕복이 N배(toxic 포함 3N). `search_many`는 `embed_documents`로 dense 임베딩을 **한 번에** 계산(모델/네트워크 왕복 절감). BM25 인덱스는 `(collection,filter)` 캐시라 재구축 없음.
- **구조:** `_dense_query`를 분리해 미리 계산한 벡터를 재사용. "port를 통해 배치를 노출" → 헥사고날 유지. 파이프는 컬렉션당 1회 호출, rerank는 조항별 유지(§10).

---

## 8. review_pipe import 버그 수정 (부수 발견)

- **발견/수정: `from contracts.ports import Retriever`는 ImportError였다** — `Retriever`는 06-30 §15에서 `adapter/port.py`로 이동됨. 즉 모듈 import 자체가 깨져 **server 기동도 실패 상태**였다.
- **조치:** `from adapter.port import Retriever, Reranker` / `from contracts.ports import Grounder, Graph`로 정정.

---

## 9. sub-chunk 메타데이터 확장 — contract_type만 채택, category 보류

- **결정 (contract_type 추가):** 서브청크 Chroma 메타데이터에 `contract_type`을 추가한다.
  - **이유:** contract_type은 **파티션 키**다 — 문서 전체가 결정하며 자식 항이 다른 유형이 될 수 없어 **무드리프트**. 혼합 컬렉션(`standard_sub_chunks`는 전 유형 적재)에서 유형 필터 없이 `top_k`를 뽑으면 타 유형이 진짜 부모를 밀어내 **recall이 깨진다**(효율이 아닌 정확도 문제). 검색 단계 `where` 필터로 사후 부모매핑 보정 의존도 제거.
  - 파이프는 서브청크 `search_many`에 `type_filter`를 넘김.
- **결정 (category 보류):** 서브청크에 category/source는 **추가하지 않는다**(현행 roll-up-to-parent 유지).
  - **이유:** 롤업 후 부모 `StandardClause`가 이미 category를 보유 → 서브청크 category는 **소비처 없는 잉여**. contract_type과 달리 "세부 항이 다른 성격"이 실재해 부모값 복사(materialize)는 **드리프트 위험**. 이는 "서브청크 단위 라우팅으로 갈 것인가"라는 조율된 결정과 묶이므로 별도 사인오프 대상.
  - **조건부:** 정말 서브청크 라우팅으로 간다면 (a) `roll_up_sub_chunks`가 category 보존/방출 (b) 메타데이터 추가 (c) 03_normalized 수작업 라벨링이 필요하며, 넣더라도 `category: Optional[Category] = None`(override 시에만 저장, 복사 금지).

---

## 10. 리랭킹 배치화 — `rerank_many` 시임만 구현 (파이프 미적용)

- **결정: 포트에 `Reranker.rerank_many`를 추가하고 구현체까지 두되, 파이프는 당분간 조항별 `rerank` 유지. 나중에 드롭인만 하면 되도록 준비.**
- **구현:** 로컬 `BgeReranker`는 전 질의의 (질의,문서) 쌍을 **flatten → `model.predict` 1회 → 순수 헬퍼 `_scatter_reranked`로 질의별 되담기**. API `ApiReranker`는 엔드포인트가 per-query 규격이라 rerank 루프(동작 동일, 네트워크 배치 불가).
- **이유:** cross-encoder는 쌍 단위 채점이라 질의별 호출이 자연스럽지만, flatten 배치는 모델 배치를 채워 호출 오버헤드를 줄인다. **각 쌍은 독립 채점 → 점수 불변(성능만 개선)**. 임베딩 왕복(주 병목)은 이미 §7로 잡혔고 rerank는 조항당 top_k(~5)만 보므로, **프로파일링에서 rerank가 병목으로 뜰 때** 파이프 교체.

---

## 11. `check_coverage` 완전 배선 — 별도 작업으로 분리 (H_check_coverage)

- **결정: 항(項) 단위 삭제 탐지(`core.check_coverage`)는 "한 줄 배선"이 아니라 조율된 기능이므로 [H_check_coverage.md](../tasks/H_check_coverage.md)로 분리한다.** ([tasks/README](../tasks/README.md)에 G·H 등록.)
- **필요 전제(현재 부재):** ① 사용자 조항의 **런타임 항 분할**(파서는 조 단위 `Clause`만 생성) ② 매칭 부모의 표준 서브청크 SQLite 로드 ③ **M×N 유사도 매트릭스** ④ 미커버 시 처리.
- **설계 정합(G_sub_chunk 3단계):** 보고는 **새 모델 필드 없이 `NONE→CHANGED` 상향**(동결 무변경). 범위는 조 내부(clause-local).
- **유사도 계산기(권장):** 이미 주입된 **`Reranker.compute_scores` 재사용**(embedder 추가 주입 회피, 크로스인코더라 정밀↑). Embedder 주입은 시그니처 변경 → 사인오프 필요.
- **정정: [G_sub_chunk.md](../tasks/G_sub_chunk.md) §4 DoD의 커버리지 체크 `[x]`는 실제와 불일치** — 순수함수 export만 됐고 미배선·무테스트. H가 담당.

---

## 기존 작업 영향 요약

| 파일 | 강도 | 내용 |
|---|---|---|
| `core/matching.py` | 🟠 신설 | `sigmoid` 순수함수 추가 |
| `core/__init__.py` | 🟡 변경 | `sigmoid`·`roll_up_sub_chunks`·`check_coverage` export |
| `adapter/port.py` | 🟠 확장 | `Retriever.search_many`·`Reranker.rerank_many` 신설 |
| `adapter/vector_manager.py` | 🟡 확장 | `_dense_query` 추출 + `search_many` 배치 구현 |
| `adapter/embedding_model.py` | 🟠 확장 | `_scatter_reranked` + `BgeReranker.rerank_many` |
| `adapter/api_embedding_model.py` | 🟠 확장 | `ApiReranker.rerank_many`(per-query 루프) |
| `pipe/review_pipe.py` | 🔴 재배선 | 배치검색·rerank·sigmoid·NO_MATCH/EXTRA·grounding 게이팅·sub-chunk·toxic·graph seam + import 버그 수정 |
| `server/server.py` | 🟡 변경 | `reranker` 주입 |
| `contracts/models.py`·`build_index.py`·`normalize.py`·migrate | 🟠 확장 | 서브청크 `contract_type` 메타데이터(§9, 일부 사용자 반영) |
| `tests/pipe/test_review_pipe.py` | 🟡 변경 | un-skip + EXTRA/NONE/toxic/sub-chunk 케이스, fake 리랭커·`search_many` |
| `tests/adapter/test_rerank_scatter.py` | 🟠 신설 | `_scatter_reranked` 순수 로직 테스트 |
| `docs/tasks/H_check_coverage.md` | 🟠 신설 | 커버리지 체크 구현 계획 |
| `docs/tasks/README.md` | 🟢 변경 | G·H 카드 등록 |
| 동결 `DeviationResult`·enum·MCP 출력 계약 | 🟢 무변경 | (`review_contract` 파라미터만 확장, §3) |

---

## 후속(미결) 항목

- **ClauseGraph(Graph) 구현** — `contracts/implement/clause_graph.py`. 없으면 `related_risk_clauses` 비활성 (F_graph).
- **check_coverage 완전 배선** — 사용자 항 분할·표준항 로드·M×N 매트릭스·`NONE→CHANGED` 상향 (H_check_coverage).
- **threshold 캘리브레이션** — `match_threshold`(sigmoid 스케일)·`coverage_threshold`(유사도 스케일) eval 기반 조정.
- **rerank_many 파이프 적용** — 프로파일링에서 rerank가 병목일 때 컬렉션당 `rerank_many`로 교체(점수 불변).
- **sub-chunk category** — 서브청크 라우팅으로 전환할 경우에만 재론(Optional override-only), 현재 보류 (§9).
- **sub-chunk 메타데이터 반영 재빌드** — `just build-db`로 `contract_type` 포함 서브청크 인덱스 재생성.
